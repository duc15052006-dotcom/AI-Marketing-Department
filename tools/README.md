# Tools Directory

Atomic Python tools invoked by marketing agents:

- All tools are typed, sandboxed, and subject to authorization gates.
- Tools enforce strict data isolation parameters (e.g. `product_id`).
- High-risk tools require authorization gate clearance prior to execution.
