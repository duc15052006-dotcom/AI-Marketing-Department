name: Apply Runtime Performance Adaptive Proposal V1

on:
  push:
    branches:
      - feat/runtime-performance-adaptive-proposal-v1
    paths:
      - ".github/workflows/runtime-performance-adaptive-proposal-apply-v1.yml"

permissions:
  contents: write

env:
  RUN_LIVE_PROVIDER_TESTS: "0"
  RUN_REAL_MODEL_TEST: "0"
  RUN_LIVE_MODEL_TESTS: "0"
  PYTHONUTF8: "1"
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  apply-production-fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact triggering head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Verify exact source identity
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ github.sha }}"
          test "$(git hash-object runtime/engine.py)" = "1369c57d7006ed3bc3ef7e9cad78204edf5b9d90"

      - name: Apply Performance adaptive proposal consumption
        run: python .github/scripts/apply_runtime_performance_adaptive_proposal_v1.py

      - name: Verify production patch scope
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          CHANGED="$(git diff --name-only)"
          printf '%s\n' "$CHANGED"
          test "$CHANGED" = "runtime/engine.py"

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install runtime dependencies
        run: |
          python -m pip install -r requirements.txt
          python -m pip check

      - name: Compile patched engine
        run: python -m py_compile runtime/engine.py

      - name: Run targeted regressions before commit
        run: |
          python -m unittest -v \
            tests.test_runtime_performance_adaptive_proposal_v1 \
            tests.test_runtime_upstream_failed_zero_tool_dispatch_adaptive_stack_v1 \
            tests.test_runtime_creative_adaptive_proposal_v1 \
            tests.test_runtime_strategist_adaptive_proposal_v1 \
            tests.test_runtime_cmo_adaptive_task_proposals_v1 \
            tests.test_runtime_dependency_scheduler_v1 \
            tests.test_creative_performance_true_handoff

      - name: Commit production fix and remove temporary apply helpers
        shell: bash
        run: |
          set -euo pipefail
          BRANCH="feat/runtime-performance-adaptive-proposal-v1"
          git fetch origin "$BRANCH"
          REMOTE_HEAD="$(git rev-parse "origin/$BRANCH")"
          test "$REMOTE_HEAD" = "${{ github.sha }}"
          rm .github/scripts/apply_runtime_performance_adaptive_proposal_v1.py
          rm .github/workflows/runtime-performance-adaptive-proposal-apply-v1.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(runtime): consume trusted Performance adaptive proposal"
          git push origin "HEAD:$BRANCH"
