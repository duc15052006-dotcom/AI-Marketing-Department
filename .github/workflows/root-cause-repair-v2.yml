name: Root Cause Repair V2 (Manual Legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "This historical self-modifying repair workflow is disabled."
          echo "Use Full Hardening V2 as the single certification authority."
