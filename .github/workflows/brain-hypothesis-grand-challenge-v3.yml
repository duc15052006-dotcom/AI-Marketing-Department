name: Brain Hypothesis Grand Challenge V3

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  holdout:
    name: Hypothesis / research-agenda future-ASI holdout V3
    runs-on: windows-latest
    timeout-minutes: 15
    env:
      RUN_LIVE_PROVIDER_TESTS: "0"
      RUN_REAL_MODEL_TEST: "0"
      RUN_LIVE_MODEL_TESTS: "0"
      AI_MARKETING_KNOWLEDGE_EPHEMERAL: "1"
      PYTHONUTF8: "1"
      PYTHONDONTWRITEBYTECODE: "1"
    steps:
      - name: Checkout exact pull request head
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Verify exact CI identity
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path .ci-results | Out-Null
          $expectedHead = "${{ github.event.pull_request.head.sha || github.sha }}"
          $actualHead = (git rev-parse HEAD).Trim()
          "expected_head_sha=$expectedHead" | Tee-Object -FilePath .ci-results/brain-hypothesis-grand-challenge-v3-identity.txt
          "actual_head_sha=$actualHead" | Tee-Object -FilePath .ci-results/brain-hypothesis-grand-challenge-v3-identity.txt -Append
          if ($actualHead -ne $expectedHead) {
            Write-Error "EXACT_HEAD_MISMATCH: expected $expectedHead but checked out $actualHead"
            exit 1
          }

      - name: Install runtime dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip check

      - name: Compile hypothesis authority surface
        shell: pwsh
        run: |
          python -m py_compile brain/hypotheses.py tests/test_brain_hypothesis_grand_challenge_v3.py

      - name: Run V3 frontier holdout
        shell: pwsh
        run: |
          python -m unittest -v tests.test_brain_hypothesis_grand_challenge_v3 `
            2>&1 | Tee-Object -FilePath .ci-results/brain-hypothesis-grand-challenge-v3.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Run adjacent canonical hypothesis suite
        if: always()
        shell: pwsh
        run: |
          python -m unittest -v tests.test_brain_hypothesis_portfolio_v1 `
            2>&1 | Tee-Object -FilePath .ci-results/brain-hypothesis-adjacent-v1.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload holdout evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: brain-hypothesis-grand-challenge-v3-${{ github.event.pull_request.head.sha || github.sha }}
          path: .ci-results/
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 14
