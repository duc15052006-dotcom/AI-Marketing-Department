name: Apply Runtime TypeError Stage Retry V1

on:
  push:
    branches:
      - fix/runtime-typeerror-stage-retry-v1
    paths:
      - ".github/workflows/runtime-typeerror-stage-retry-apply.yml"
      - ".github/scripts/apply_typeerror_stage_retry_v1.py"

permissions:
  contents: write

env:
  RUN_LIVE_PROVIDER_TESTS: "0"
  RUN_REAL_MODEL_TEST: "0"
  RUN_LIVE_MODEL_TESTS: "0"
  PYTHONUTF8: "1"
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Verify exact preimage
        shell: bash
        run: |
          test "$(git rev-parse HEAD)" = "${{ github.sha }}"
          actual="$(git hash-object runtime/engine.py)"
          expected="73fb310f9050a08dea11282421602b998a4c2f68"
          echo "expected_engine_blob=$expected"
          echo "actual_engine_blob=$actual"
          test "$actual" = "$expected"
      - name: Apply guarded patch
        run: python .github/scripts/apply_typeerror_stage_retry_v1.py
      - name: Verify patch scope
        shell: bash
        run: |
          git diff --check
          test "$(git diff --name-only | tr -d '\r')" = "runtime/engine.py"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt
      - name: Install dependencies
        run: |
          python -m pip install -r requirements.txt
          python -m pip check
      - name: Run targeted regression before commit
        run: |
          python -m unittest -v \
            tests.test_runtime_typeerror_stage_retry_v1 \
            tests.test_runtime_intelligence_adaptive_fanout_v1
      - name: Commit production fix and remove temporary apply harness
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/runtime-typeerror-stage-retry-apply.yml
          git rm .github/scripts/apply_typeerror_stage_retry_v1.py
          git add runtime/engine.py
          git diff --cached --check
          git commit -m "fix(runtime): avoid retrying stages on internal TypeError"
          git push origin HEAD:fix/runtime-typeerror-stage-retry-v1
